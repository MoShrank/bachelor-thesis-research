import numpy as np


class TemporalPooler:
    def __init__(self, no_cells: int, no_columns: int):

        self.no_cells = no_cells
        self.no_columns = no_columns

        # connections to other cells in the same column

        # permanences of connections to other cells in the same column

        # state of cells

        # time step

    def compute(self, winning_columns: np.ndarray):
        pass
